import React from "react";
import Image from "./Image";

export default function Card({ id, name, price, rating }) {
    return (
        <li className="w-full p-6 bg-white-500 shadow-sm">
            <Image src="" />
        </li>
    );
}
